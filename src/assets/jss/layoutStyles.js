const layoutStyles = (theme) => ({
  background: {
    backgroundColor: "#051933",
    backgroundSize: "cover",
    height: "100vh",
    width: "100%",
    overflow: "auto",
  },
});

export default layoutStyles;
